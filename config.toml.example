# reorg-playground configuration file

# Database path of the key value store. Will be created if non-existing.
database_path = "dbfile"

# Interval in seconds for checking for new blocks
query_interval = 15

# Webserver listen address
address = "127.0.0.1:2323"

# RSS feeds need a URL of the site. This is optional. If unset,
# the RSS feeds might not be valid according to the RSS 2.0 specification.
# Some RSS readers might complain.
rss_base_url = ""

[[networks]]
id = 1 # identifier in DB
name = "Mainnet"
description = ""
min_fork_height = 0
max_interesting_heights = 100
network_type = "Mainnet" # Mainnet | Testnet | Signet | Regtest

    [[networks.nodes]]
    id = 0 # identifier in DB
    name = "Node A"
    description = "A node with a signet-miner that is always mining blocks"
    implementation = "" # bitcoincore | btcd | esplora | electrum

    rpc_host = "127.0.0.1"
    rpc_port = 38342
    rpc_user = ""
    rpc_password = ""
    # rpc_cookie_file = "~/.bitcoin/.cookie"


    [[networks.nodes]]
    id = 1 # identifier in DB
    name = "Node B"
    description = "A node with a signet-miner that is always mining blocks"
    implementation = "" # bitcoincore | btcd | esplora | electrum

    rpc_host = "127.0.0.1"
    rpc_port = 38332
    rpc_user = ""
    rpc_password = ""
    # rpc_cookie_file = "~/.bitcoin/.cookie"


    [[networks.nodes]]
    id = 2
    name = "Electrum Emzy"
    description = "URL electrum.emzy.de:50002"
    implementation = "electrum"

    rpc_host = "ssl://electrum.emzy.de"
    rpc_port = 50002

[[networks]]
id = 2
name = "testnetwork"
description = ""
min_fork_height = 5
max_interesting_heights = 200
network_type = "Testnet" # Mainnet | Testnet | Signet | Regtest

    [[networks.nodes]]
    id = 0
    name = "Node A"
    description = "A node. Just A node."
    
    rpc_host = "127.0.0.1"
    rpc_port = 38342
    rpc_user = ""
    rpc_password = ""
    # rpc_cookie_file = "~/.bitcoin/.cookie"
